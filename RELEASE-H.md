# 9.24-H — 主播首頁自訂

管理 → 設定 → 自訂首頁，可調整主題色、主圖、首頁卡片的顯示與排序，以及本日狀態的文字和欄位。桌機支援拖曳，手機可用上下按鈕。原主圖、自訂 HTTPS 圖片／上傳、無圖可切換。

每位主播獨立儲存在既有主播設定。未設定的首頁維持原版面，隱藏模組不刪除資料或關閉功能。主題保留深色介面及可讀的文字對比。700px 以下使用手機排列。

61 項測試及 Build 通過。瀏覽器驗證儲存、重新載入、排序、冠歌隱藏與恢復、主圖隱藏、狀態文字、待播數量，390px 無橫向溢出。後端驗證拒絕錯誤設定且業務資料不變。圖片上傳沿用現有壓縮及儲存流程，未向正式空間上傳測試圖片。

後端先部署相容更新；前端再發布。無資料庫 migration 或歷史紀錄重寫。前一可回復版本為 release/9.24-B.2（7dac8c191fb5682f786853ca3fa8f740b1de0cee）。後端完整包使用 node build-edge.mjs 生成。

此批不代表私訊留言、完整勳章、卡片牆追加需求、在線監控或真實手機鎖屏 Push 已完成。
