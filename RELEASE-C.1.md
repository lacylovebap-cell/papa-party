# 9.24-C.1 — 玩家與主播私訊

玩家登入後按 Header「💬 私訊」即可與目前主播交談；管理後台可搜尋共用玩家，開啟或回覆本主播的對話。提供未讀列表、已讀回條、歷史分頁、文字訊息與通知串接。前景對話每四秒更新，並在既有通知 Realtime 更新時同步刷新。

對話按玩家 × 主播隔離，後端每次驗證登入與權限。一般主播限自己的空間；總管理可切主播查看。前台會告知主播與總管理可查看。通知不包含私訊本文。送出使用唯一編號防止網路重試重複，並限制每位發送者每對話兩秒一筆。

新資料表啟用 RLS，僅 service_role 能存取，瀏覽器經現有 Edge API。訊息與通知在同一資料庫交易寫入，操作歷史只記中繼資料。已讀不刪訊息；總管理與主播的未讀分開。沒有重寫原有點歌、存歌、玩家、卡片與冠歌資料。

另修正 Edge bundle 內 URL 變數覆蓋標準網址解析器的問題，確保主圖及 Web Push 訂閱驗證可正常處理 HTTPS URL。

67 項測試、Build 通過；編譯後完整後端測試、正式測試主播與測試玩家雙向收訊、重試去重、已讀、通知、匿名／跨玩家／跨主播拒絕、匿名資料表存取拒絕均通過。手機 390px 無橫向溢出，私訊中的 HTML 以純文字顯示。資料庫 migration 為 202609250002_private_chat.sql，先更新資料庫與後端，再發布前台。

此批是 Release C 的私訊部分。全站／主播留言板、指定／排除對象、匿名留言與公告仍為下一批；勳章 E 與真實手機鎖屏 Push 驗收未因此完成。

回復點：release/9.24-H，commit 2a3a39529e2b4c649829044de3d8433437f18ecd。回復前台時保留相容後端與私訊資料表，勿刪除已產生對話或用舊資料快照覆蓋。
